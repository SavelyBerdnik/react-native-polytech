import { View, Text } from 'react-native';
import { styles } from './Styles';

export const APIScreen = () => (
    <View style={styles.container}>
        <Text style={styles.text}>API Layout</Text>
    </View>
);